import { Link } from 'react-scroll';

const Header = () => {
    return (
        <header className="bg-green-400 text-white w-full h-12 fixed top-0 z-50 shadow-lg">
            <div className="container mx-auto flex justify-between items-center p-4">
            </div>
        </header>
    );
}

export default Header;